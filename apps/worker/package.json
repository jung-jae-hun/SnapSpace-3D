{
  "name": "@snapspace/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "lint": "eslint .",
    "lint:ci": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "bullmq": "^5.76.1",
    "ioredis": "^5.10.1",
    "minio": "^8.0.7"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.3"
  }
}
